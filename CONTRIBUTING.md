# Contributing

欢迎提交改进建议与 PR。请优先遵守以下约定：

1. 避免提交任何本机路径、私有文档内容、真实密钥。
2. 所有路径耦合信息集中在 skill 根目录下的 `configs/`。
3. 有必要时需要补充 `README.md` 与 `EXPORT_MANIFEST.md` 的说明。

## 提交前检查

- `README.md` 步骤可执行；
- 阶段 00 的缺配置提示与模板文件保持一致；
- 扫描无敏感信息（`TOKEN`、本机绝对路径等）。
